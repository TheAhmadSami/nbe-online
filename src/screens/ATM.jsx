import { Button, Text, View, Image } from 'react-native';
import styles from './../styles/ATM.style'

export const ATM = ({ navigation }) => {
  return (
    <View style={styles.container}>
      <Text>Welcome to ATM Page</Text>
    </View>
  );
}