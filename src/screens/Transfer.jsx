import { Button, Text, View, Image } from 'react-native';
import styles from './../styles/Transfer.style'

export const Transfer = ({ navigation }) => {
  return (
    <View style={styles.container}>
      <Text>Welcome to Transfer Page</Text>
    </View>
  );
}