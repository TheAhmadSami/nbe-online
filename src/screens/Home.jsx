import { Button, Text, View, Image } from 'react-native';
import styles from './../styles/Home.style'

export const Home = ({ navigation }) => {
  return (
    <View style={styles.container}>
      <Text>Welcome to Hi Page</Text>
    </View>
  );
}