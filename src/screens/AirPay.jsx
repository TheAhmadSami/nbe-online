import { Button, Text, View, Image } from 'react-native';
import styles from './../styles/AirPay.style'

export const AirPay = ({ navigation }) => {
  return (
    <View style={styles.container}>
      <Text>Welcome to AirPay Page</Text>
    </View>
  );
}